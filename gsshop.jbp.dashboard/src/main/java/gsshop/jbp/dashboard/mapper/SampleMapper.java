package gsshop.jbp.dashboard.mapper;

import gsshop.jbp.dashboard.dto.Sample;

// TODO 여기서 부터 구현해야 함 !
public class SampleMapper {
	
	//@Select("select * from sample where email = #{email}")
	//Sample findByEmaile(@Param("email") String email);
	
}
